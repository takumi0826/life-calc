import { WarekiDisplayPipe } from './wareki-display.pipe';

describe('WarekiDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new WarekiDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
