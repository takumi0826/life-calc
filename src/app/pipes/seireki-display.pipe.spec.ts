import { SeirekiDisplayPipe } from './seireki-display.pipe';

describe('SeirekiDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SeirekiDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
